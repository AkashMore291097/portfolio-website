node_modules
dist
dist-ssr
*.local

.env
.env.*
!.env.example

.vscode/*
!.vscode/extensions.json
.idea

npm-debug.log*
yarn-debug.log*
yarn-error.log*

*.tsbuildinfo

# Claude Code local state (not project content; CLAUDE.md itself stays tracked)
.claude/

# Project process files, no longer tracked in git per explicit request
backlog/
docs/
